# vim: set ft=yaml:
---
- inventory:
    raspi:
      - heimbot.fritz.box
    esel:
      - esel.fritz.box

- hosts: raspi
  properties:
    - name: Checking SSH
      ssh: { port: 22, version: 2.0, software: "OpenSSH.*" }

